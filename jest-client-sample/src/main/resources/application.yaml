server:
  port: 8081
  servlet:
    context-path: /web
spring:
  http:
    converters:
      preferred-json-mapper: gson
  elasticsearch:
    jest:
      uris:
        - http://localhost:9207
      kerberos:
        username: kerberos
        enabled: false
